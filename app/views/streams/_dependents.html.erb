Choose a Feed:
<%= label :feed, "id", "Choose a Feed:" %>
<%= select 'feed', 'id', options_from_collection_for_select(@feeds, 'id', 'name') %>

<%= label :twitter_account, "id", "Choose a Twitter Account:" %>
<%= select 'twitter_account', 'id', options_from_collection_for_select(@twitter_accounts, 'id', 'handle') %>
